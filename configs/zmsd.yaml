# This is an example of a configuration file.
# Please configure any targets that you wish to push data to.
# Note that not all targets support every source.
#
# server_config: /etc/zabbix/zabbix_server.conf
# buffer_size: 100
# plugins_dir: /usr/lib/zms/plugins    # Directory containing plugin .so files
# http:
#   listen_address: localhost
#   listen_port: 2020
# tag_filters:
#   accepted:
#   - tag: <name>
#     value: <value>
#   rejected:
#   - tag: <name>
#     value: <value>
# targets:
# - name: <unique_name>
#   type: pushgateway|azuretable|print|other plugin
#   connection: <connectionstring>
#   options:                       # Plugin-specific options (optional)
#     custom_option: value
#   tag_filters:
#     accepted:
#     - tag: <name>
#       value: <value>
#     rejected:
#     - tag: <name>
#       value: <value>
#   source:
#   - history
#   - trends
#   - events
#
# # Example plugin configuration:
# - name: file_output
#   type: plugin:file_observer
#   connection: /tmp/zms_files
#   source:
#   - history
#   - trends
#
# # Example log_print plugin configuration:
# # Filters and outputs only LOG type history items to stdout/stderr
# - name: log_output
#   type: log_print
#   connection: stdout              # or "stderr"
#   exports:
#   - history
